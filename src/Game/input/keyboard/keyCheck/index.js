// Whirl.input.keyboard.keyCheck

const keyIsDown = require("./keyIsDown.js");
const keyIsUp = require("./keyIsUp.js");

const keyCheck = {
	keyIsDown,
	keyIsUp
};

module.exports = keyCheck;
